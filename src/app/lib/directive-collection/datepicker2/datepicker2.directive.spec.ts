import { Datepicker2Directive } from './datepicker2.directive';

describe('Datepicker2Directive', () => {
  it('should create an instance', () => {
    const directive = new Datepicker2Directive();
    expect(directive).toBeTruthy();
  });
});
